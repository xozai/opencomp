{
  "name": "@opencomp/quotas",
  "version": "0.1.0",
  "private": true,
  "description": "Quotas module — quota assignment, versioning, period management, and attainment tracking",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/contracts": "workspace:*",
    "@opencomp/platform-audit": "workspace:*",
    "@opencomp/participants": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
