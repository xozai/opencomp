{
  "name": "@opencomp/payouts",
  "version": "0.1.0",
  "private": true,
  "description": "Payouts module — payout records produced from calculation runs, adjustment application, payroll export",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/calculations": "workspace:*",
    "@opencomp/contracts": "workspace:*",
    "@opencomp/platform-audit": "workspace:*",
    "@opencomp/platform-events": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
