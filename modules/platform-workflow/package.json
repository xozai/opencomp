{
  "name": "@opencomp/platform-workflow",
  "version": "0.1.0",
  "private": true,
  "description": "Workflow engine — state machine for approval flows, plan publishing, dispute routing",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/contracts": "workspace:*",
    "@opencomp/platform-audit": "workspace:*",
    "@opencomp/platform-events": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
