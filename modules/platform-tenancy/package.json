{
  "name": "@opencomp/platform-tenancy",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenancy — tenant resolution, context propagation, tenant-scoped DB access",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/contracts": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "fastify": "^4.27.0",
    "fastify-plugin": "^4.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
