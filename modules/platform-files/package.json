{
  "name": "@opencomp/platform-files",
  "version": "0.1.0",
  "private": true,
  "description": "File storage abstraction — local disk and S3-compatible storage for uploads and generated documents",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/contracts": "workspace:*",
    "@aws-sdk/client-s3": "^3.575.0",
    "mime-types": "^2.1.35",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
