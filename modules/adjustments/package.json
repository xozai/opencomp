{
  "name": "@opencomp/adjustments",
  "version": "0.1.0",
  "private": true,
  "description": "Adjustments module — manual payout adjustments resulting from approved disputes or exceptions",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencomp/approvals": "workspace:*",
    "@opencomp/contracts": "workspace:*",
    "@opencomp/payouts": "workspace:*",
    "@opencomp/platform-audit": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
